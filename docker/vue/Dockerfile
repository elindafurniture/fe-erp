FROM node:23.7-alpine AS builder

ARG VITE_APP_BACKEND_URL
ARG VITE_APP_BACKEND_URL_MASTER
ENV VITE_APP_BACKEND_URL=$VITE_APP_BACKEND_URL
ENV VITE_APP_BACKEND_URL_MASTER=$VITE_APP_BACKEND_URL_MASTER

WORKDIR /app
COPY . .

RUN npm install
RUN npm run build

CMD ["sleep", "infinity"]